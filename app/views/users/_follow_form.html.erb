<% unless current_user?(@user) %>
    <div id="follow_form">
        <% if current_session_user.following?(@user) %>
            <%= render 'unfollow' %>
        <% else %>
            <%= render 'follow' %>
        <% end %>
    </div>
<% end %>